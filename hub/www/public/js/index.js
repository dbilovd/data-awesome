/**
 * index.js
 */

var graph = require("./src/modules/graph/graph.js");
	//embed = require("./src/modules/embed/embed.js"),

// Initialise embed
// embed.init();

// Initialise and run graph app
graph.init();